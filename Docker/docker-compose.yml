services:
  gcc:
    image: debian:bookworm
    container_name: 32_compiler-c
    working_dir: /srcs
    stdin_open: true
    tty: true
    volumes:
      - ./32_srcs:/srcs:z
    command: bash -c "dpkg --add-architecture i386 && apt-get update && apt-get install -y gcc gcc-multilib && bash"